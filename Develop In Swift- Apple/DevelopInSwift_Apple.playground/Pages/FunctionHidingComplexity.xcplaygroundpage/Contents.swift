//: [Previous](@previous)

import Foundation

/*
 Main point of using functions is to break things down into understandable, reusable parts. Each part does a very clear piece of work.
 */

/*
 But the point of using functions is to break things down into understandable, reusable parts. Each part does a very clear piece of work.
 */

/*
 When working on an app, you’re never looking at every single line of code. You’ll call a function knowing what it does, but not necessarily how it does it. For example, all of the song creation functions on this page have been built into the page already, so you can write the song very simply
 */
